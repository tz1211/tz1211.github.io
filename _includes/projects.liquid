<div class="col">
  <div class="card h-100 hoverable">
    {% if project.img %}
      {%
        include figure.liquid
        loading="eager"
        path=project.img
        sizes = "250px"
        alt="project thumbnail"
        class="card-img-top"
      %}
    {% endif %}
    <div class="card-body d-flex flex-column">
      <h2 class="card-title">{{ project.title }}</h2>
      <p class="card-text">{{ project.description }}</p>
      <div class="project-links d-flex justify-content-between align-items-center w-100 mt-auto pt-2 ps-3 pe-4">
        {% if project.paper %}
          <a class="btn btn-outline-primary fw-bold" href="{{ project.paper }}" target="_blank" rel="noopener noreferrer">Paper</a>
        {% endif %}
        {% if project.post %}
          <a class="btn btn-outline-primary fw-bold" href="{{ project.post }}" target="_blank" rel="noopener noreferrer">Post</a>
        {% endif %}
        {% if project.github or project.code %}
          <a class="btn btn-outline-primary fw-bold" href="{{ project.github | default: project.code }}" target="_blank" rel="noopener noreferrer">Code</a>
        {% endif %}
      </div>
      {% if project.github_stars %}
        <div class="github-icon mt-1">
          <span class="stars" data-toggle="tooltip" title="GitHub Stars">
            <i class="fa-solid fa-star"></i>
            <span id="{{ project.github_stars }}-stars"></span>
          </span>
        </div>
      {% endif %}
    </div>
  </div>
</div>
